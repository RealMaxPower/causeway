// Stub for the `server-only` marker package. The real one throws when
// imported in a client bundle to enforce the boundary. In Vitest we just
// want it to be importable. Aliased via vitest.config.ts.
export {};

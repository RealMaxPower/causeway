import Link from "next/link";
import { L1, L1QA, L2, L2Grid, L2Step, Callout, L3, L3Grid, Debate, DebateSide, Sources, Check } from "@/components/layers";
import { RegimeComposer } from "@/components/widgets/regime-composer";

export const meta = {
  heroClaim: {
    lbl: "The framing",
    text: "Macro regimes aren't predicted, they're read — the way you read weather. Watch four indicators; when three or more agree, you have your read.",
    sub: "Inflation, money, labor, credit. The composer below pulls each live from FRED so you can see today's regime, not last quarter's.",
  },
  heroStatsLabel: "By the numbers",
  heroStats: [
    { lbl: "Indicators the composer tracks", val: "4", sub: "Inflation, money, labor, credit — each live from FRED, refreshed monthly." },
    { lbl: "Threshold for a clear regime read", val: "3 of 4", sub: "Three or more confirming axes = a clear read. Fewer = the regime is in transition." },
  ],
  relatedNodes: ["H2", "C5", "F1"],
};

export const L1Body = (
  <L1 title="The thirty-second answer">
    <div>
      <L1QA question="What is this?">
        Four orthogonal axes — inflation, money, labor, credit — combine into a regime read.
        Three of four agreeing is a high-confidence read; two is a turning point. The signal
        you want is which way the wind is blowing before you make a decision, not what
        someone says about it after.
      </L1QA>
      <L1QA question="Why should I care?">
        Every Track H decision — saving, housing, career, debt — is regime-dependent.
        Reading the regime first means each later question is a smaller question.
        Skipping it means re-litigating every decision from scratch.
      </L1QA>
    </div>
    <div
      style={{
        border: "1px solid var(--rule)",
        borderRadius: "var(--r-md)",
        padding: 18,
        background: "var(--paper-2)",
      }}
    >
      <div
        style={{
          fontFamily: "var(--cw-mono)",
          fontSize: 10.5,
          letterSpacing: "0.14em",
          color: "var(--ink-3)",
          textTransform: "uppercase",
          marginBottom: 12,
        }}
      >
        Open the live read
      </div>
      <p
        style={{
          fontFamily: "var(--cw-serif)",
          fontSize: 15,
          color: "var(--ink-2)",
          lineHeight: 1.55,
          margin: "0 0 14px",
        }}
      >
        Four FRED indicators are wired — CPI YoY (CPIAUCSL), the effective fed funds
        rate (FEDFUNDS), U-3 unemployment (UNRATE), and SLOOS net-tightening (DRTSCILM).
        The dashboard updates each time FRED publishes.
      </p>
      <Link
        href="/regime"
        style={{
          fontFamily: "var(--cw-mono)",
          fontSize: 12,
          letterSpacing: "0.08em",
          color: "var(--gold-deep)",
        }}
      >
        Open the regime dashboard →
      </Link>
    </div>
  </L1>
);

export const L2Body = (
  <L2 title="Four axes, read one at a time">
    <L2Grid>
      <L2Step num="01" title="Pick orthogonal axes, not collinear ones">
        <p className="lede">
          A regime read is only useful if the axes can disagree. Inflation, money, labor,
          and credit can each be loose or tight independently — and the interesting moments
          are when they do.
        </p>
        <p>
          Blending them into one number hides the disagreement, which is where the
          information actually is. Report each axis alone; let the composite be the
          count of axes confirming a theme.
        </p>
      </L2Step>
      <L2Step num="02" title="Cheap thresholds beat clever models">
        <p>
          A dashboard built for decision support doesn&apos;t need a hidden-state filter —
          it needs <em>thresholds you can defend in a sentence</em>. Inflation against the
          Fed&apos;s target, fed funds against r*, unemployment against its long-run
          average. The post-1990 sample is calibration enough.
        </p>
      </L2Step>
      <L2Step num="03" title="Turning points are the point">
        <p>
          Three-of-four agreeing is a recognisable regime; the decisions in Track H
          ride on it. Two-of-four is the more interesting state — that&apos;s when the
          regime is about to flip and the canonical playbook stops working. The dashboard
          should make turning points <strong>obvious</strong>, not hide them in a
          smoothed average.
        </p>
      </L2Step>
      <Callout
        title="Read the regime before you decide anything else."
        link={{ to: "/regime", text: "Regime dashboard · live" }}
      >
        <p>
          When the regime is clear, most Track H questions answer themselves. When the
          regime is turning, the same questions need different answers than they did six
          months ago. Either way, this is the first stop.
        </p>
      </Callout>
    </L2Grid>

    <RegimeComposer />
  </L2>
);

export const L3Body = (
  <L3 title="History, debate, sources">
    <L3Grid
      sources={
        <Sources
          ids={[
            "bis-borio-financial-cycle-2014",
            "fed-monetary-policy-report",
            "fred-drtscilm",
            "nber-business-cycle-dating",
            "romer-romer-monetary-shocks-2004",
            "bridgewater-all-weather",
          ]}
        />
      }
    >
      <h3>Where this framework comes from</h3>
      <p>
        The four-axes-with-tally read isn&apos;t something a single economist
        invented. It crystallised over the 2000s out of three traditions
        running in parallel. <strong>Bridgewater&apos;s All-Weather work</strong>{" "}
        formalised the growth × inflation quadrant for portfolio construction
        in the 1990s. <strong>The BIS</strong> (notably Borio&apos;s financial-
        cycle work after 2008) made the credit-cycle axis a first-class object
        alongside the macro cycle. <strong>The Fed&apos;s own dashboards</strong>{" "}
        treat inflation, money, and labor as the three legs of the dual-mandate
        read; adding credit completes the picture by capturing the financial-
        plumbing dimension the dual mandate explicitly excludes.
      </p>

      <h3>Why a tally, not a composite index</h3>
      <p>
        The temptation when you have four indicators is to weight and blend them
        into one number. Don&apos;t. <em>Composite indices hide the disagreement,
        and the disagreement is where the information actually is.</em> The
        Conference Board&apos;s Leading Economic Index, the Chicago Fed&apos;s
        National Financial Conditions Index, even the Fed&apos;s own
        recession-probability models all blend; all turn out to be most wrong
        at exactly the turning points where the simpler four-axis read makes
        the gap visible.
      </p>
      <p>
        The tally preserves the structure. Three of four agreeing tells you{" "}
        <em>which</em> theme is winning and which axis is the holdout. Two of
        four tells you the regime is mid-transition. A single blended number
        loses both signals in the same gesture.
      </p>

      <Debate question="Is the four-axis read a forecast, or a current-state diagnostic?">
        <DebateSide side="mainstream" who="● Diagnostic only">
          <p>
            The dashboard reports what&apos;s happening now, not what&apos;s
            coming. Calling future turning points is a separate (much harder)
            problem the four axes don&apos;t solve. Treat the read as a snapshot
            for positioning decisions; don&apos;t pretend it&apos;s predictive.
          </p>
        </DebateSide>
        <DebateSide side="heterodox" who="● Embedded forecast">
          <p>
            The axes <em>are</em> implicitly forward-looking — SLOOS leads bank
            loan growth by 1-2 quarters, unemployment lags policy by 12-18
            months, inflation lags rate moves further still. Reading the
            current configuration is reading a partially-revealed future. Deny
            that and you lose the operational value.
          </p>
        </DebateSide>
      </Debate>

      <h3>The honest middle position</h3>
      <p>
        Both sides are partly right. The dashboard is unambiguously a current-
        state diagnostic when you ask &quot;what is the regime now?&quot;.
        It&apos;s also partially forward-looking because the axes have built-in
        lags relative to the policy variable that drove them. Treating it as
        either pure snapshot OR pure forecast is wrong; the right framing is
        &quot;a snapshot of variables that respond to policy at different
        speeds, some of which are already telling you what the next read will
        look like.&quot;
      </p>

      <h3>Edge cases worth flagging</h3>
      <p>
        <strong>Reading the tally during a supply shock.</strong> 2022 produced
        a configuration the four-axis framework wasn&apos;t calibrated for —
        inflation tight, money loose-then-rapidly-tight, labor tight, credit
        easy. The tally read 2-of-4 for most of 2022, which suggested a turning
        point. It WAS a turning point, but for reasons the dashboard couldn&apos;t
        see (energy spike, fiscal hangover) rather than the conventional
        endogenous-cycle reasons. Honest users of the framework noted that the
        turning-point signal was right and the underlying mechanism was different.
      </p>
      <p>
        <strong>Single-country reads in a global regime.</strong> The US
        dashboard treats the US economy as self-contained. When global
        conditions diverge sharply (China deflating while the US runs hot,
        Eurozone recession while US grows), the US-only read can mislead about
        the regime US assets actually face. Cross-check the headline read
        against{" "}
        <a href="/compare?c=USA,CHN,DEU&i=FP.CPI.TOTL.ZG,BN.CAB.XOKA.GD.ZS" style={{ color: "var(--gold-deep)" }}>
          comparable indicators across major economies
        </a>{" "}
        before betting heavily on a single-country reading.
      </p>

      <Check
        question="Three of the four regime axes (inflation, money, labor, credit) are signalling 'easing'; one is mixed. How should you interpret this?"
        options={[
          {
            label: "Wait until all four agree — three-out-of-four isn't statistically significant.",
            explain: "There's no statistical-significance threshold being applied here; the framing is interpretive. Three-of-four is exactly the configuration that supports a high-confidence regime read."
          },
          {
            label: "High-confidence read — three of four agreeing is the strong regime signal. The mixed axis is worth watching, but the dominant theme is clear; act on it and let the laggard catch up.",
            correct: true,
            explain: "Right. Three-of-four is the operational threshold for confidence. Two-of-four (split) is the turning-point signal that says the playbook needs reassessment. Waiting for unanimity means acting after the move has already happened."
          },
          {
            label: "Inconclusive — any disagreement means the regime isn't really an easing one.",
            explain: "Too restrictive. Regimes are messy at the edges; the indicators are noisy. The framework explicitly tolerates partial confirmation precisely because perfect agreement is rare in real time."
          }
        ]}
        seeAlso={{ href: "/regime", label: "the live regime dashboard" }}
      />
    </L3Grid>
  </L3>
);
